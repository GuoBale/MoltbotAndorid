{
  "name": "openclaw-android-bridge",
  "version": "1.0.0",
  "description": "OpenClaw Gateway extension for Android Bridge Service",
  "type": "module",
  "main": "dist/android-bridge.js",
  "types": "dist/android-bridge.d.ts",
  "scripts": {
    "build": "tsc && cp openclaw.plugin.json dist/ && cp -r skills dist/",
    "watch": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "openclaw",
    "android",
    "bridge",
    "gateway"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
